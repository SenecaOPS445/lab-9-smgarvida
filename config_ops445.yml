---
- name: Configure OPS445 Linux VM
  hosts: smgarvida
  become: yes
  tasks:
    - name: Update Apache (httpd)
      yum:
        name: httpd
        state: latest

    - name: Install EPEL repository
      yum:
        name: epel-release
        state: present

    - name: Remove tree package
      yum:
        name: tree
        state: absent

    - name: Set hostname
      hostname:
        name: smgarvida

    - name: Create new user with sudo access
      user:
        name: smgarvida
        groups: wheel
        state: present

    - name: Set up SSH for new user
      authorized_key:
        user: smgarvida
        state: present
        key: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"

    - name: Create directory structure for labs
      file:
        path: "/home/smgarvida/ops445/lab{{ item }}"
        state: directory
        owner: smgarvida
        group: smgarvida
      loop:
        - 1
        - 2
        - 3
        - 4
        - 5
        - 6
        - 7
        - 8
        - 9

